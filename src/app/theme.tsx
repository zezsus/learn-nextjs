/** @format */
"use client";

import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    secondary: {
      main: "#ffffff",
    },
    info: {
      main: "#666666",
    },
  },
});

export default theme;
